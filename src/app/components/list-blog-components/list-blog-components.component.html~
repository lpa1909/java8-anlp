<h2>List Blogs</h2>
<nz-table #basicTable>
  <thead>
  <tr>
    <th>STT</th>
    <th>Tin</th>
    <th>Loại</th>
    <th>Trạng thái</th>
    <th>Vị trí</th>
    <th>Ngày public</th>
    <th>Chi tiết</th>
    <th>Chỉnh sửa</th>
    <th>Xóa</th>
  </tr>
  </thead>
  <tbody>
  <tr *ngIf="blogsToDisplay.length === 0">
    <h2>Không có tin tức mà bạn đang tìm kiếm</h2>
  </tr>
  <tr *ngFor="let data of blogsToDisplay; let i = index">
    <td>{{ i + 1 }}</td>
    <td>{{ data.title }}</td>
    <td>{{ getCategoryName(data.category) }}</td>
    <td [ngStyle]="{'color': data.public ? 'blue' : 'red'}">{{ data.public ? "Yes" : "No" }}</td>
    <td>
      <nz-tag *ngFor="let positionName of getPositionNames(data.position)">
        {{ positionName }}
      </nz-tag>
    </td>
    <td>{{ data.data_pubblic | date:"dd/MM/yyyy" }}</td>
    <td style="text-align: center">
      <button><span nz-icon nzType="eye" nzTheme="outline"></span></button>
    </td>
    <td style="text-align: center">
      <button><span nz-icon nzType="edit" nzTheme="outline"></span></button>
    </td>
    <td>
      <button (click)="deleteBlog(data.id)"><span nz-icon nzType="delete" nzTheme="outline"></span></button>
    </td>
  </tr>
  </tbody>

</nz-table>
