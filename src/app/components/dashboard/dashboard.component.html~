<h3>Top 4 movies</h3>
<div>
  <a *ngFor="let movie of movies" class="col-1-4">
    <div class="movie-box">
      <a routerLink="/detail/{{ movie.id }}" class="link-movie">
        <h4>{{ movie.name }}</h4>
      </a>
    </div>
  </a>
</div>
